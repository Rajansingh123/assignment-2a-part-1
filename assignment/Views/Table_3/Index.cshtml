@model IEnumerable<assignment.Models.Table_3>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Table_1.cars)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.company)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Table_1.cars)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.company)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cars }) |
            @Html.ActionLink("Details", "Details", new { id=item.cars }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cars })
        </td>
    </tr>
}

</table>
